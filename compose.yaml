services:
  hashcards:
    build: .
    ports:
      - "3001:3000"
    volumes:
      - ./config.json:/app/config.json:ro
      - ./example:/app/data
      - ./tools/menu.sh:/app/menu.sh:ro # option
    restart: unless-stopped
